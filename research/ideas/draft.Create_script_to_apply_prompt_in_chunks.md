- Many times a prompt is either a loop around files or a list of actions (e.g., a
  sequence of commands), but the LLM is not able to execute
- Create a loop controlling the agent when there are this situations
