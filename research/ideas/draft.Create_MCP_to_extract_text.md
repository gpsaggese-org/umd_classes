We want to have a way to easily extract and inject chunks of markdown text
in skills

We already have all the code to extract the markdown

Maybe use an approach similar to render_images where text is copy-pasted
with a link back to the source

Then have a function to update the files (like render_images.py)
