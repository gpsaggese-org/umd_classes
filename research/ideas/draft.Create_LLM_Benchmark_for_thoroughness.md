Similar problem to needle-in-haystack

When a prompt has a lot of things to do (or there are many points to apply a rule)
the LLM doesn't always follow up

Create a benchmark (e.g., from applying coding.rules.md) for this

Run multiple LLMs and see which one is more thorough
