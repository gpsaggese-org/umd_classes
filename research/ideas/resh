#!/bin/bash -e
# """
# Open the research/ideas help README.
# """

# Find the directory of this script.
SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"

README="$SCRIPT_DIR/README.md"
if [[ ! -f "$README" ]]; then
    echo "ERROR: Could not find $README" >&2
    exit 1
fi

vi "$README"
