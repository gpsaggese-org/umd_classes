In convert_pdf_to_md.py convert --clean_only into --action 
using the action idiom
where the actions are "convert", "remove_junk"

The input is only a pdf file, not an md

- When writing code you must always follow the instructions in
  `@.claude/skills/coding.rules.md`
