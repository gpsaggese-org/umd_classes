% ##############################################################################
\section{NoesisServer: fulfillment and monitoring}
\label{sec:noesis_server}

\NoesisServer{} is the layer that turns a matched contract
(Definition~\ref{def:contract}) into actual inference requests against
real providers, and turns the resulting traffic into three things: a
measurement of whether the contract was honored, a signal for
difficulty-aware routing, and a corpus for downstream distillation.

% ==============================================================================
\subsection{Gateway architecture}
\label{sec:gateway_architecture}

\NoesisServer{} is, at its core, a lightweight LLM API gateway modeled on
multi-provider routers such as OpenRouter: it exposes a single API,
proxies each request to one or more underlying providers, and streams the
response back to the caller. Two properties distinguish it from a plain
pass-through router. First, every request is tagged with the contract
$\kappa$ it is served under, if any, so that fulfillment can be attributed
(Section~\ref{sec:fulfillment_monitoring}). Second, every prompt/response
pair is logged with its provider, model, measured latency, measured cost,
and, when available, a user feedback signal, regardless of whether the
request was placed under a contract. The storage schema is intentionally
minimal:

$$
  (\text{prompt}, \text{response}, \text{provider}, \text{model},
   \text{latency}, \text{cost}, \kappa\text{ or } \bot)
$$

This logging is passive by default: it does not change routing behavior
and exists purely to build a reusable dataset for evaluation and
distillation (Section~\ref{sec:distillation}). Privacy and data-handling
safeguards (PII scrubbing, opt-in/opt-out) are necessary before logging
real traffic and are discussed as an open question in
Section~\ref{sec:open_questions}.

Figure~\ref{fig:request_path} traces this path end to end.

% rendered_images:begin
% ```graphviz
% digraph RequestPath {
%   rankdir=LR;
%   splines=spline;
%   nodesep=0.7;
%   ranksep=0.9;
%   bgcolor="white";
% 
%   node [fontname="Helvetica", fontsize=12, style="rounded,filled", shape=box, penwidth=1.3];
%   edge [fontname="Helvetica", fontsize=10, color="#4A4A4A", penwidth=1.2, arrowsize=0.8];
% 
%   Caller [label="Caller\n(application / agent)", fillcolor="#F4A6A6"];
%   Gateway [label="API gateway\n(NoesisServer)", fillcolor="#A0D6D1"];
%   Provider [label="Provider", fillcolor="#C6A6F4"];
%   Log [label="Log:\n(prompt, response, provider, model,\nlatency, cost, contract-or-bottom)", shape=note, fillcolor="#A6C8F4"];
% 
%   Caller -> Gateway [label="  request  "];
%   Gateway -> Provider [label="  request  "];
%   Provider -> Gateway [label="  response  ", style=dashed];
%   Gateway -> Caller [label="  response  ", style=dashed];
%   Gateway -> Log [
%     label="  logged on the way back  ",
%     color="#B23A48",
%     fontcolor="#B23A48",
%     penwidth=1.4,
%     style=dashed,
%     constraint=false
%   ];
% }
% ```
% label=fig:request_path
% caption=Path of a single request through \NoesisServer{}. The caller's
%   request is proxied to a provider and the response streamed back; on the
%   way back, the request is logged as the tuple (prompt, response,
%   provider, model, latency, cost, contract-or-$\bot$) described in
%   Section~\ref{sec:gateway_architecture}.
% rendered_images:end
% render_images:begin
\begin{figure}[H]
  \includegraphics[width=\linewidth]{05_noesis_server.tex.figs/05_noesis_server.1.png}
  \caption{Path of a single request through \NoesisServer{}. The caller's request is proxied to a provider and the response streamed back; on the way back, the request is logged as the tuple (prompt, response, provider, model, latency, cost, contract-or-$\bot$) described in Section~\ref{sec:gateway_architecture}.}
  \label{fig:request_path}
\end{figure}
% render_images:end

% ==============================================================================
\subsection{Contract fulfillment monitoring}
\label{sec:fulfillment_monitoring}

For a request $x$ served under contract $\kappa$, let $\hat c(x) \in
\calC$ denote the capability tier actually delivered and $\hat\ell(x)$ the
actually measured latency, both estimated from the response (e.g., via a
verifier model, a benchmark probe, or agreement with a stronger reference
model). Request $x$ is a \emph{success} under $\kappa$ if

\begin{equation}
  \label{eq:success_indicator}
  s(x) = \mathbb{1}\!\left[
    \hat c(x) \succeq \clevelattr(\kappa)
    \;\wedge\;
    \hat\ell(x) \leq \lmaxattr(\kappa)
  \right].
\end{equation}

\begin{definition}[Measured reliability]
\label{def:measured_reliability}
For a fulfillment window $W$, the set of requests served under contract
$\kappa$, the measured reliability is
\begin{equation}
  \label{eq:measured_reliability}
  \hat r(\kappa, W) = \frac{1}{|W|} \sum_{x \in W} s(x).
\end{equation}
\end{definition}

A naive violation rule, flagging $\kappa$ whenever
$\hat r(\kappa, W) < \rminattr(\kappa)$, conflates genuine under-delivery
with sampling noise: $\hat r(\kappa, W)$ is a sample average of an
unobserved true success rate, and a small $|W|$ makes false accusations
likely. We instead treat fulfillment monitoring as a one-sided hypothesis
test: the null hypothesis $H_0$ is that the seller is compliant, i.e., its
true success rate is at least $\rminattr(\kappa)$, and $\kappa$ is flagged
as violated only if $H_0$ is rejected at a chosen confidence level
$1 - \delta$. A simple normal-approximation lower confidence bound gives

\begin{equation}
  \label{eq:reliability_lower_bound}
  \hat r_{\text{lower}}(\kappa, W)
  = \hat r(\kappa, W)
  - z_{1 - \delta}
    \sqrt{
      \frac{\hat r(\kappa, W) \, (1 - \hat r(\kappa, W))}{|W|}
    },
\end{equation}
where $z_{1 - \delta}$ is the corresponding standard normal quantile, and
$\kappa$ is flagged as violated when
$\hat r_{\text{lower}}(\kappa, W) < \rminattr(\kappa)$. For small $|W|$, an
exact interval (e.g., Clopper--Pearson) is preferable to the normal
approximation used here for exposition. Flagged violations are reported
to \NoesisMarket{} and feed the reputation update of
Equation~\eqref{eq:reputation_update}.

\begin{example}[Fulfillment reporting]
\label{ex:fulfillment_reporting}
A contract promising 99.9\% reliability at \texttt{frontier} capability is
served by three providers over a window of 5,000 requests. \NoesisServer{}
computes $\hat r(\kappa, W) = 0.994$ and, applying
Equation~\eqref{eq:reliability_lower_bound}, finds
$\hat r_{\text{lower}}(\kappa, W) < 0.999$: the shortfall is unlikely to be
sampling noise, so the contract is flagged as violated and reported back
to \NoesisMarket.
\end{example}

Figure~\ref{fig:reliability_test} places the point estimate, its lower
confidence bound, and the threshold on a single number line for both
outcomes of the test.

% rendered_images:begin
% ```tikz
% \definecolor{flagred}{RGB}{244,166,166}
% \definecolor{okgreen}{RGB}{178,226,178}
% \fill[flagred, opacity=0.12] (0,-1) rectangle (6,3.4);
% \fill[okgreen, opacity=0.12] (6,-1) rectangle (12,3.4);
% \draw[dashed, thick, black!70] (6,-1) -- (6,3.4);
% \node[above] at (6,3.4) {\footnotesize $R_{\min}(\kappa)$};
% \draw[very thick, teal!60!black] (7.2,2.4) -- (10.4,2.4);
% \draw[very thick, teal!60!black] (7.2,2.25) -- (7.2,2.55);
% \draw[very thick, teal!60!black] (10.4,2.25) -- (10.4,2.55);
% \filldraw[black] (9.8,2.4) circle (2pt);
% \node[below] at (7.2,2.1) {\footnotesize $\hat r_{\text{lower}}$};
% \node[above] at (9.8,2.7) {\footnotesize $\hat r(\kappa,W)$};
% \node[align=right, teal!60!black] at (-1.9,2.4) {\footnotesize compliant:\\[-1pt]\footnotesize attributed to noise};
% \draw[very thick, red!60!black] (3.6,0) -- (7.6,0);
% \draw[very thick, red!60!black] (3.6,-0.15) -- (3.6,0.15);
% \draw[very thick, red!60!black] (7.6,-0.15) -- (7.6,0.15);
% \filldraw[black] (7.0,0) circle (2pt);
% \node[below] at (3.6,-0.3) {\footnotesize $\hat r_{\text{lower}}$};
% \node[above] at (7.0,0.3) {\footnotesize $\hat r(\kappa,W)$};
% \node[align=right, red!60!black] at (-1.9,0) {\footnotesize flagged:\\[-1pt]\footnotesize violation reported};
% ```
% label=fig:reliability_test
% caption=The hypothesis test of Equation~\eqref{eq:reliability_lower_bound}
%   on a single number line. Top: the confidence interval
%   $[\hat r_{\text{lower}}, \hat r(\kappa, W)]$ stays entirely above
%   $\rminattr(\kappa)$, so the shortfall, if any, is attributed to
%   sampling noise. Bottom: the interval dips below $\rminattr(\kappa)$, so
%   $\kappa$ is flagged as violated and reported to \NoesisMarket.
% rendered_images:end
% render_images:begin
\begin{figure}[H]
  \includegraphics[width=\linewidth]{05_noesis_server.tex.figs/05_noesis_server.2.png}
  \caption{The hypothesis test of Equation~\eqref{eq:reliability_lower_bound} on a single number line. Top: the confidence interval $[\hat r_{\text{lower}}, \hat r(\kappa, W)]$ stays entirely above $\rminattr(\kappa)$, so the shortfall, if any, is attributed to sampling noise. Bottom: the interval dips below $\rminattr(\kappa)$, so $\kappa$ is flagged as violated and reported to \NoesisMarket.}
  \label{fig:reliability_test}
\end{figure}
% render_images:end

% ==============================================================================
\subsection{Difficulty-aware routing}
\label{sec:routing}

Not every request needs the strongest available model. \NoesisServer{}
trains a difficulty estimator
\begin{equation}
  \label{eq:difficulty_estimator}
  \hat d : \calP \to \calC
\end{equation}
mapping a prompt $x \in \calP$ to the cheapest capability tier expected to
answer it correctly, and routes according to
\begin{equation}
  \label{eq:routing_policy}
  \pi(x) = \min \{ c \in \calC : c \succeq \hat d(x) \}.
\end{equation}

Routing is only worthwhile if the estimator is cheaper than the savings it
produces. Let $\text{cost}(c)$ denote the per-request cost of serving tier
$c$, non-decreasing in $c$, let $c_K$ denote the strongest tier, and let
$c_{\hat d}$ denote the per-request cost of evaluating $\hat d$ itself.
Routing yields a net saving over an always-route-to-$c_K$ baseline exactly
when

\begin{equation}
  \label{eq:routing_saving_condition}
  c_{\hat d} + \EE_x [\, \text{cost}(\pi(x)) \,]
  <
  \text{cost}(c_K).
\end{equation}

Inequality~\eqref{eq:routing_saving_condition} makes explicit the trap
flagged in the underlying design notes: an estimator that is itself
expensive, or inaccurate enough to route hard requests to weak tiers and
force costly retries, can cost more than always calling the strong model.
Measuring the left-hand side against the right-hand side on real traffic
is a prerequisite for deploying routing in production, and is deferred to
the implementation plan in Section~\ref{sec:roadmap}.

Figure~\ref{fig:routing_flowchart} lays out this chain next to the
condition under which it pays off.

% rendered_images:begin
% ```graphviz
% digraph Routing {
%   rankdir=LR;
%   splines=spline;
%   nodesep=0.7;
%   ranksep=0.9;
%   bgcolor="white";
% 
%   node [fontname="Helvetica", fontsize=12, style="rounded,filled", shape=box, penwidth=1.3];
%   edge [fontname="Helvetica", fontsize=10, color="#4A4A4A", penwidth=1.2, arrowsize=0.8];
% 
%   Prompt [label="prompt x", fillcolor="#FFD1A6"];
%   Estimator [label="difficulty estimator\n(cheapest tier expected\nto answer correctly)", fillcolor="#A0D6D1"];
%   Tier [label="routed tier π(x)\n(cheapest sufficient tier)", fillcolor="#A6C8F4"];
%   Cost [label="net saving only if:\nestimator cost + expected routed cost\n< always-strong-model cost", shape=note, fillcolor="#F7F9FC", color="#B8C4D9"];
% 
%   Prompt -> Estimator;
%   Estimator -> Tier;
%   Tier -> Cost [style=dashed, color="#8C8C8C", constraint=false];
% }
% ```
% label=fig:routing_flowchart
% caption=Difficulty-aware routing. A prompt $x$ is mapped by the
%   difficulty estimator to the cheapest capability tier $\pi(x)$ expected
%   to answer it correctly (Equation~\eqref{eq:routing_policy}); routing
%   only pays off when the estimator's own cost plus the expected routed
%   cost undercuts always calling the strongest tier
%   (Inequality~\eqref{eq:routing_saving_condition}).
% rendered_images:end
% render_images:begin
\begin{figure}[H]
  \includegraphics[width=\linewidth]{05_noesis_server.tex.figs/05_noesis_server.3.png}
  \caption{Difficulty-aware routing. A prompt $x$ is mapped by the difficulty estimator to the cheapest capability tier $\pi(x)$ expected to answer it correctly (Equation~\eqref{eq:routing_policy}); routing only pays off when the estimator's own cost plus the expected routed cost undercuts always calling the strongest tier (Inequality~\eqref{eq:routing_saving_condition}).}
  \label{fig:routing_flowchart}
\end{figure}
% render_images:end

% ==============================================================================
\subsection{Answer fusion}
\label{sec:fusion}

Fusion is the complementary lever to routing. Instead of picking one tier
per request, \NoesisServer{} may query a set of providers $S(x) \subseteq
\calM$ and combine their answers through an aggregator
\begin{equation}
  \label{eq:fusion_aggregator}
  \hat y(x) = g\big( \{ f_m(x) \}_{m \in S(x)} \big),
\end{equation}
where $g$ may be a majority vote, a verifier model that scores and selects
among candidates, or a learned combiner trained on logged outcomes.

Routing and fusion trade cost for accuracy in opposite directions relative
to a single-strong-model baseline: routing accepts a higher risk of a
lower-quality answer on some fraction of requests in exchange for a lower
expected cost, while fusion accepts a higher expected cost, from querying
multiple providers per request, in exchange for a lower risk of a
low-quality answer. Which lever is preferable for a given contract depends
on the contract's price versus its $\rminattr$ term: a low-price,
low-$\rminattr$ contract favors routing, while a high-$\rminattr$ contract
may only be economically served through fusion.

Figure~\ref{fig:fusion_fanout} contrasts fusion's fan-out/fan-in shape
with routing's single-provider path.

% rendered_images:begin
% ```graphviz
% digraph Fusion {
%   rankdir=LR;
%   splines=spline;
%   nodesep=0.5;
%   ranksep=0.7;
%   bgcolor="white";
% 
%   node [fontname="Helvetica", fontsize=11, style="rounded,filled", shape=box, penwidth=1.3];
%   edge [fontname="Helvetica", fontsize=10, color="#4A4A4A", penwidth=1.2, arrowsize=0.8];
% 
%   subgraph cluster_routing {
%     label="Routing (single path)";
%     fontname="Helvetica-Bold";
%     fontsize=12;
%     style="rounded,filled";
%     fillcolor="#F7F9FC";
%     color="#B8C4D9";
%     margin=16;
%     PromptR [label="prompt x", fillcolor="#FFD1A6"];
%     ProviderR [label="one provider\n(routed tier)", fillcolor="#C6A6F4"];
%     AnswerR [label="answer", fillcolor="#A6C8F4"];
%     PromptR -> ProviderR -> AnswerR;
%   }
% 
%   subgraph cluster_fusion {
%     label="Fusion (fan-out / fan-in)";
%     fontname="Helvetica-Bold";
%     fontsize=12;
%     style="rounded,filled";
%     fillcolor="#F7F9FC";
%     color="#B8C4D9";
%     margin=16;
%     PromptF [label="prompt x", fillcolor="#FFD1A6"];
%     P1 [label="provider in S(x)", fillcolor="#C6A6F4"];
%     P2 [label="provider in S(x)", fillcolor="#C6A6F4"];
%     P3 [label="provider in S(x)", fillcolor="#C6A6F4"];
%     Aggregator [label="aggregator g\n(vote / verifier / learned)", fillcolor="#A0D6D1"];
%     AnswerF [label="answer", fillcolor="#A6C8F4"];
%     PromptF -> P1;
%     PromptF -> P2;
%     PromptF -> P3;
%     P1 -> Aggregator;
%     P2 -> Aggregator;
%     P3 -> Aggregator;
%     Aggregator -> AnswerF;
%   }
% }
% ```
% label=fig:fusion_fanout
% caption=Answer fusion as a fan-out/fan-in, contrasted with routing's
%   single-provider path. Fusion sends the same prompt to every provider in
%   $S(x)$ and combines their answers through the aggregator $g$
%   (Equation~\eqref{eq:fusion_aggregator}), trading extra inference cost
%   for a lower risk of a low-quality answer.
% rendered_images:end
% render_images:begin
\begin{figure}[H]
  \includegraphics[width=\linewidth]{05_noesis_server.tex.figs/05_noesis_server.4.png}
  \caption{Answer fusion as a fan-out/fan-in, contrasted with routing's single-provider path. Fusion sends the same prompt to every provider in $S(x)$ and combines their answers through the aggregator $g$ (Equation~\eqref{eq:fusion_aggregator}), trading extra inference cost for a lower risk of a low-quality answer.}
  \label{fig:fusion_fanout}
\end{figure}
% render_images:end

% ==============================================================================
\subsection{Training signal and distillation}
\label{sec:distillation}

The difficulty estimator of Equation~\eqref{eq:difficulty_estimator}
needs a training signal, which the logged traffic supplies for free.

\begin{definition}[Agreement label]
\label{def:agreement_label}
For a prompt $x$ answered by both a cheap model $f_{\text{cheap}}$ and a
strong model $f_{\text{strong}}$, the agreement label is
\begin{equation}
  \label{eq:agreement_label}
  y(x) = 1 - \mathbb{1}\big[ f_{\text{cheap}}(x) \equiv f_{\text{strong}}(x) \big],
\end{equation}
where $\equiv$ denotes exact match or a similarity threshold, used as a
proxy label for ``this prompt was hard'' ($y(x) = 1$) versus ``easy''
($y(x) = 0$).
\end{definition}

Definition~\ref{def:agreement_label} is a proxy, not a ground-truth
correctness label: the two models can agree on a wrong answer, or
disagree while the cheap model happens to be correct. A genuine quality
label requires independent verification, for instance from a pairwise
preference arena, which is a companion research direction discussed in
Section~\ref{sec:related_work}. With that caveat, the logged corpus
$$
  \calD = \{ (x_i, f_{\text{strong}}(x_i)) \}_{i=1}^N
$$
also supports knowledge distillation of a smaller model $f_\theta$ trained
to approximate $f_{\text{strong}}$'s outputs on the observed task
distribution \cite{hinton2015}, offering a second, independent way for
\NoesisServer{} to reduce cost on traffic it has already observed.

Figure~\ref{fig:training_pipeline} shows the two consumers that share
this one logged dataset.

% rendered_images:begin
% ```graphviz
% digraph TrainingPipeline {
%   rankdir=LR;
%   splines=spline;
%   nodesep=0.6;
%   ranksep=0.8;
%   bgcolor="white";
% 
%   node [fontname="Helvetica", fontsize=12, style="rounded,filled", shape=box, penwidth=1.3];
%   edge [fontname="Helvetica", fontsize=10, color="#4A4A4A", penwidth=1.2, arrowsize=0.8];
% 
%   Traffic [label="logged prompt/response\ntraffic", fillcolor="#FFD1A6"];
%   Agreement [label="agreement labels\n(cheap vs. strong model)", fillcolor="#B2E2B2"];
%   Estimator [label="difficulty estimator\ntraining", fillcolor="#A0D6D1"];
%   Corpus [label="corpus D\n(x, strong-model answer)", fillcolor="#B2E2B2"];
%   Distill [label="distillation of\na smaller model", fillcolor="#A0D6D1"];
% 
%   Traffic -> Agreement;
%   Agreement -> Estimator;
%   Traffic -> Corpus;
%   Corpus -> Distill;
% }
% ```
% label=fig:training_pipeline
% caption=The training pipeline built from one logged dataset. Logged
%   traffic feeds agreement labels (Definition~\ref{def:agreement_label}),
%   which train the difficulty estimator of
%   Section~\ref{sec:routing}; the same traffic also forms the corpus
%   $\calD$ used to distill a smaller model $f_\theta$
%   (Section~\ref{sec:distillation}), as two parallel, independent
%   consumers of one dataset.
% rendered_images:end
% render_images:begin
\begin{figure}[H]
  \includegraphics[width=\linewidth]{05_noesis_server.tex.figs/05_noesis_server.5.png}
  \caption{The training pipeline built from one logged dataset. Logged traffic feeds agreement labels (Definition~\ref{def:agreement_label}), which train the difficulty estimator of Section~\ref{sec:routing}; the same traffic also forms the corpus $\calD$ used to distill a smaller model $f_\theta$ (Section~\ref{sec:distillation}), as two parallel, independent consumers of one dataset.}
  \label{fig:training_pipeline}
\end{figure}
% render_images:end
