# Keep the Docker image minimal. Tutorial-specific AI deps are installed
# directly from notebook cells.

ipykernel==6.30.1
jupyterlab==4.4.6
nbclient==0.10.2
nbformat==5.10.4
python-dotenv
nest_asyncio
